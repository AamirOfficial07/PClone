# Tests

This folder is reserved for unit and integration test projects, as described in the design docs.

Suggested structure:

- `SocialOrchestrator.UnitTests/`
- `SocialOrchestrator.IntegrationTests/`

These can be created with `dotnet new xunit` or your preferred test template in later phases.