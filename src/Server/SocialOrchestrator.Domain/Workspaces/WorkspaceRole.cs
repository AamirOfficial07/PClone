namespace SocialOrchestrator.Domain.Workspaces
{
    /// <summary>
    /// Enum defining the roles a user can have within a workspace.
    /// </summary>
    public enum WorkspaceRole
    {
        Owner = 1,
        Admin = 2,
        Member = 3
    }
}
